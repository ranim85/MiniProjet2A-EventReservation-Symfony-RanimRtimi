{% extends 'admin/base.html.twig' %}

{% block title %}Éditer Événement{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Modifier : {{ event.title }}</h1>
        <div>
            <a href="{{ path('admin_event_index') }}" class="btn btn-outline-glass btn-sm me-2">Retour</a>
            {{ include('admin/event/_delete_form.html.twig') }}
        </div>
    </div>

    {{ include('admin/event/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
{% endblock %}
